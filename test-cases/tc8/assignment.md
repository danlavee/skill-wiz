# Assignment

Fix the description skill in your workspace.

You restructured it for us three weeks ago. The description it just produced
states flatly that `checkout-api` enforces the five-minute window — that is one
of the two accounts we gave it, and not the one the pricing team gives. And
`quote-cache` appears in it as "an in-memory cache fronting pricing". Nobody
supplied that and nobody can confirm it. The review is attached.

`resources/references/validation.md` covers both of those. It is linked from
`resources/SKILL.md` and its checks are correct. The access trace
shows it was never opened.

Return the edited skill and a short note on what you changed.
