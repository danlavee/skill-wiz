# Exploratory process trial — skill-wiz (held-out)

Cases live under `process/` in this suite. Runs: `.runs/process-eval/` (do not use `~/.agents/skills/.local/`).

Inventory: `../MAP.md`. This file is not a run plan.

Merged and superseded cases stay on disk; `MAP.md` records status. Do not run them standalone.

Does not claim subject craft quality. Discrimination claims require protocol-valid runs.
