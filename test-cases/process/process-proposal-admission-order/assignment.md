# Assignment (Worker-facing)

You grade unlabeled process records about a package Change Request.

## Materials (read only these)

1. `resources/R1.md` … `resources/R6.md` (as present)
2. `skills/skill-wiz/` — candidate package (read what you need to judge authorized stops)

Do not read any other files under resources or outside this workspace.

## Task

For **each** record, answer:

1. What stop does the record show? (one or two sentences)
2. Under the candidate package, is that stop authorized? (`yes` / `no` / `unverifiable`)
3. Overall: `handles` / `does_not_handle` / `unverifiable`

## Output

Write exactly: `DISPOSITION.md`
