# Assignment (Worker-facing — full brief)

You grade two process run records against the same fixtures and package.
You do not edit the package.

## Materials (read only these)

1. `resources/retained-evaluation-clean.md`
2. `resources/outside-challenge.md`
3. `resources/records/R1.md`
4. `resources/records/R2.md`
5. `skills/skill-wiz/references/run-and-grade.md` — section **Decide and present** (including any **Residual** paragraph)
6. `skills/skill-wiz/SKILL.md` — core tenet **Evaluation Channels** only

Do not read any other files under resources or outside this workspace.

## Task

The fixtures are shared context for both records.

For **each** of R1 and R2, answer with `yes` / `no` / `unverifiable` and cite the record (and package only if the record relies on it):

| Id | Question |
|---|---|
| Q1 | Did this process run identify a fault in the evaluation governor (skill-wiz process), not only in subject craft under test? |
| Q2 | Was that fault tied to the outside challenge in the fixtures (the signal that disputes the clean disposition), rather than only to a different fault story? |
| Q3 | Did this process run establish a process-side repair path for that governor fault (process case, grade, and repair work on skill-wiz), rather than only subject re-runs or package edits without a named process fault? |
| Q4 | Did this process run claim success from package wording alone, without a process path that addresses the outside challenge? |

Then overall for each record: does this run **handle** the fixture situation (outside challenge after clean) as a governor process matter with a process path? Answer `handles` / `does_not_handle` / `unverifiable`.

## Output

Write exactly one file:

`DISPOSITION.md`

No preferred outcome. Do not invent facts not in the records or package spans you cite.
