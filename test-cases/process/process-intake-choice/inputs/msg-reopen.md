# User message M-reopen (Worker-facing)

Prior work was mid subject craft thrash (diagram re-runs).

User: `That was the wrong residual. The failure is in Wiz process — how we re-enter after my message — not more subject craft. Stop thrashing the subject.`
