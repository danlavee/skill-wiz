# User message M-continue (Worker-facing)

Prior work was already bound: entry = process Cases on a frozen problem case; next step was write the case files.

User: `yes. create the files.`

(No new residual, no rebuke of path, no apply/stop, no “you’re doing craft instead of process.”)
