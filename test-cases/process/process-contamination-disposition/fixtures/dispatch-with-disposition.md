# Fixture: dispatch context B

A case was assembled. Before activation, a contamination disposition was retained that:

1. Listed every Worker-facing path and the activation prompt  
2. Listed evaluator-only artifacts held out of the Worker workspace  
3. Recorded pass on provenance and referential closure for each Worker-facing surface  

Only after that pass disposition were Workers activated.
