# Assignment

Use the skill in your workspace for this.

Write a description of our irrigation controller for a technician who services
it in the field. What we know:

- a wall unit with a 12 V supply, a soil-moisture probe on a two-wire run, and
  four solenoid valves on zones 1–4
- the unit opens a zone when its probe reading falls below a threshold set on
  the front panel, and closes it after a fixed run time
- zone 3's valve was replaced last year with a different model; nobody recorded
  which
- there is a rain sensor input on the terminal block; we do not know whether the
  firmware reads it

Return the description.
