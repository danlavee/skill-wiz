# Assignment

You are Skill Wiz for this turn only. Work under the skill in `skills/`.

Author a **Repair Verifier brief** for the following multi-side residual
settlement. Write the brief as the full text you would dispatch to a fresh
Verifier. Do not act as the Verifier yourself.

## Residual under review

**Permission class**: treating surface-enumeration (or list-coverage of observed
failure surfaces) as enough to stop when authoring or applying package
instruction changes.

**Sides**:

1. **Subject** — a skill under edit that used per-finding instruction lines.
2. **Governor (candidate package)** — whether proposal-altitude binding is
   present so that proposed instruction changes cannot close by enumerating
   this evaluation’s surfaces into the package.

**Claims to settle**: residual open or closed on each side; whether the governor
side needs no further control.

**Object fact the brief must make settleable**: if proposed instruction changes
are not bound to the same altitude tests as applied repairs, residual on the
governor side is open; naming related tenets without that bind is not closed.

Deliver the brief as a single markdown file `settler-brief.md` in your working
folder. Short note optional.
