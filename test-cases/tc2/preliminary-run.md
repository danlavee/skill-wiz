# Assignment

Use the skill in your workspace for this.

Draw a diagram of our badge-access path for the security handbook. The reader
sits at the door, the door controller in the ceiling void, the access server in
the comms room. The reader sends the badge ID to the controller, the controller
asks the server, the server answers allow or deny, and the controller drives the
strike. The reader also reports tamper straight to the server, bypassing the
controller.

Return the diagram and its source.
